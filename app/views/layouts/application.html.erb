<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnly</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <%# these flash hashes are added per the Devise install recommendation %>
    <% if flash[:notice] %>
      <div class="">      
        <p class=""><%= notice %></p> 
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class=''>
        <p class=''><%= alert %></p>
      </div>
    <% end %>

  <h1>This is Members Only - madafaka</h1>

  <div class='field is-grouped'>
              <p class='control'>
                <%= link_to 'New Post', new_post_path, class: 'button is-info is-inverted' %>
              </p>
              <% if user_signed_in? %>
                <p class='control'>
                  <%= link_to current_user.username, edit_user_registration_path, class: 'button is-info' %>
                </p>
                <p>
                  <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: "delete" }, class: 'button is-info' %>
                </p>
              <% else %>
                <p class='control'>
                  <%= link_to 'Log in', new_user_session_path, method: :get, class: 'button is-info' %>
                </p>
                <p class='control'>
                <%= link_to 'Sign up', new_user_registration_path, class: 'button is-info' %>
                </p>
              <% end %>
            </div>

  <body>
    <%= yield %>
  </body>
</html>
